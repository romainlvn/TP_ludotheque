spring:
    application:
        name: TP_ludotheque

    datasource:
        url: jdbc:sqlserver://localhost;databasename=ludo_db;encrypt=false
        username: sa
        password: "Pa$$w0rd"

    jpa:
        show-sql: true
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true
